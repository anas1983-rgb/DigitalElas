<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFAF8"
    android:fillViewport="true">

    <!-- ⬇️ ÚNICO FILHO DO SCROLLVIEW -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <!-- Cabeçalho Compacto -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnVoltarQuiz"
                android:layout_width="60dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Voltar"
                android:src="@android:drawable/ic_menu_revert"
                android:tintMode="@color/laranja"/>

            <TextView
                android:id="@+id/txtProgressoQuiz"
                android:layout_width="304dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:gravity="center"
                android:text="Pergunta 1 de 10"
                android:textColor="#8B4789"
                android:textSize="22sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/btnOuvirQuiz"
                android:layout_width="60dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Ouvir pergunta"
                android:src="@android:drawable/ic_media_play"
                app:tint="@color/laranja" />

        </LinearLayout>

        <!-- Barra de Progresso -->
        <ProgressBar
            android:id="@+id/progressBarQuiz"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:progress="0"
            android:max="10"
            android:progressTint="#8B4789" />

        <!-- Pergunta -->
        <TextView
            android:id="@+id/txtPerguntaQuiz"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="18dp"
            android:background="@color/white"
            android:elevation="20dp"
            android:gravity="center"
            android:padding="22dp"
            android:text="Qual é a pergunta do quiz?"
            android:textColor="#333333"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- ✅ NOVO: Texto do Áudio em Tempo Real -->
        <TextView
            android:id="@+id/txtAudioEmTempoReal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="20sp"
            android:gravity="center"
            android:textColor="#666666"
            android:background="#F0F0F0"
            android:padding="20dp"
            android:layout_marginBottom="16dp"
            android:visibility="gone"
            android:maxLines="3"
            android:ellipsize="end" />

        <!-- Opções de Resposta -->
        <RadioGroup
            android:id="@+id/radioGroupOpcoes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="80dp"
            android:orientation="vertical">

            <!-- Os RadioButtons serão adicionados dinamicamente -->

        </RadioGroup>

        <!-- Explicação (inicialmente invisível) -->
        <TextView
            android:id="@+id/txtExplicacao"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="20sp"
            android:gravity="start"
            android:textColor="#4CAF50"
            android:layout_marginBottom="16dp"
            android:padding="16dp"
            android:background="#F3E5F5"
            android:visibility="gone" />

        <!-- Botões de Navegação - Layout Vertical para Mobile -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <Button
                android:id="@+id/btnAvancarQuiz"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Próxima"
                android:textSize="22sp"
                android:background="#4CAF50"
                android:textColor="#FFFFFF"
                android:visibility="gone"
                android:tint="#8B4789"/>

        </LinearLayout>

        <Button
            android:id="@+id/btnProximaPergunta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:background="#8B4789"
            android:text="Ver Resposta"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:tint="#8B4789" />

    </LinearLayout>
    <!-- FIM DO ÚNICO FILHO -->

</ScrollView>
